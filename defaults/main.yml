---

db_dump_relative_path: "wp-content/uploads/ithemes-security/backups"

wp_use_ssl: False

ssl_certificate_file: "/etc/ssl/private/{{ ansible_nodename }}/selfsigned.crt"
ssl_certificate_key_file: "/etc/ssl/private/{{ ansible_nodename }}/privkey.pem"
